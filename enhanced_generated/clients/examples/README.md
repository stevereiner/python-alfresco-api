# ⚠️ Generated Examples Directory - DO NOT USE

## 🚫 Warning: These Examples Are Not Compatible

The examples in this directory and its subdirectories are **auto-generated by openapi-generator-cli** and are **NOT compatible** with this project's master client architecture.

### ❌ What's Wrong With These Examples:

- **Import Errors**: They use imports like `from alfresco_auth import AuthClient` which don't exist
- **Standalone Design**: They're designed for individual API clients, not the unified master client
- **Auto-Generated**: They regenerate with each build, so any fixes would be lost
- **Legacy Pattern**: They use outdated patterns not supported by this project

### ✅ Use These Instead:

**Main Examples Directory:**
```bash
# Master client examples (recommended)
python examples/master_client_examples.py

# Enhanced master client examples
python examples/master_client_usage.py

# Individual API examples (compatible)
python examples/auth_examples.py
python examples/core_examples.py
python examples/discovery_examples.py
# ... and more
```

### 🔧 If You Want to Fix Generated Examples (Not Recommended):

1. **Change imports** from:
   ```python
   from alfresco_auth import AuthClient
   ```
   
   To:
   ```python
   import sys
   import os
   sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..', '..'))
   from enhanced_generated.AlfrescoClient import AlfrescoClient
   ```

2. **Change client initialization** from:
   ```python
   client = AuthClient(base_url="...", username="...", password="...")
   ```
   
   To:
   ```python
   client = AlfrescoClient(host="...", username="...", password="...")
   # Then use: client.auth.method_name()
   ```

**But seriously,** just use the curated examples in `examples/` directory instead! 🚀

### 📚 Documentation

- **[Main README](../../../README.md)** - Complete project documentation
- **[Master Client Guide](../../../docs/MASTER_CLIENT_GUIDE.md)** - How to use the unified client
- **[Examples Directory](../../../examples/)** - Working, tested examples

---

**Remember:** These generated files will be **overwritten** on the next build. Use the main examples directory for reliable, maintained examples. 